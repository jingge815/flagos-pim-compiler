# 测试集精简研发记录（2026-09-01）

## 修改内容

真实 Llama-2-7B 策略测试默认只保留 `tp4_pp2`。移除的真实 7B 参数组合为 `tp8_pp1`、`tp2_pp4` 和 `tp1_pp8`。`tp4_pp2` 同时包含张量并行和流水并行，验证编译、权重加载、KV 缓存和自回归生成。

保留 `tests/test_executor_llama2_7b.py`。该测试执行一次真实 7B 的完整 prefill 执行计划，将 logits 与单卡 PyTorch 输出对拍，并比较每个位置的贪心 token。它验证整图执行计划，和解码循环测试的目标不同。

所有 TP/PP 组合仍在 `tests/test_strategy_sweep.py` 的小模型测试中覆盖，包括 `tp4_pp1`、`tp2_pp2`、`tp1_pp4`；该测试适合进入常规 pytest 集合。

## 结果日志和默认命令

pytest 会将最近一次结果写到 `test-results/pytest-latest.md`。日志列出测试文件、测试项、状态、耗时（秒）和汇总。

默认执行命令：

```bash
source /media/disk/fengjingge/src/flagOS/flagOS-installed/pytorch/env-pytorch.sh
python -m pytest tests/ -x -q
```

按需聚焦验证真实 7B 代表链路或轻量策略遍历时，可执行：

```bash
python -m pytest tests/test_strategy_llama2_7b.py -q
python -m pytest tests/test_strategy_sweep.py -q
```

当前环境已配置真实 Llama-2-7B 权重、FlagTree 安装目录和 GeneSim 根目录。完整默认集已实际运行真实 7B 与 GeneSim 测试。

恢复 prefill 测试后的定向验证结果：`tests/test_executor_llama2_7b.py` 为 2 passed，耗时 56.83 秒。该测试验证的 logits 与 PyTorch 输出满足数值误差阈值，且每个位置的贪心 token 一致。

最近一次完整默认集结果为 264 passed，0 skipped，总耗时 846.14 秒。真实 7B 的 `tp4_pp2` 编排器解码、KV 对拍、自然语言生成、完整 prefill 执行计划和已编译线性内核端到端测试均通过。

## 尚未覆盖的项目

- IR dump、serialize、replay。
- 实际 GeneSim 二进制端到端运行。
- SDK 语义一致性和 PU 映射的专门端到端测试。
